{
  "name": "kangaroo-post",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter ./packages/shared-types run build && pnpm -C apps/web build && pnpm -C firebase/functions build",
    "typecheck": "tsc -b",
    "dev:web": "pnpm -C apps/web dev",
    "lint": "eslint .",
    "prepare:functions": "pnpm -C packages/shared-types build && pnpm -C firebase run sync:types",
    "dev:functions": "pnpm run prepare:functions && pnpm -C firebase/functions serve",
    "emulators": "pnpm run prepare:functions && pnpm -C firebase emulators",
    "a8:sync": "pnpm -C firebase/functions exec tsx src/jobs/a8/ingestFromFiles.ts",
    "a8:norm": "pnpm -C firebase/functions exec tsx src/jobs/a8/normalizeA8Offers.ts",
    "a8:ui": "pnpm -C firebase/functions exec tsx src/jobs/a8/seedUiFromIngest.ts"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "@types/yargs": "^17.0.34",
    "tsx": "^4.20.6",
    "typescript": "^5.3.3",
    "yargs": "^18.0.0"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "@google-cloud/tasks": "^6.2.1",
    "firebase-admin": "^12.5.0"
  }
}
